<%= form_with(model: [training, training_comment], url: public_training_training_comments_path(training), method: :post, local: true) do |f| %>
  <%= f.text_area :comment, rows:'5',placeholder: "コメントをここに", class: "w-100" %>
  <%= f.submit "送信" %>
<% end %>